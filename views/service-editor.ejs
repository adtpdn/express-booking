<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= filename %></title>
    <link href="/styles/style.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <%- include('partials/navbar') %>

    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold mb-6">Edit <%= filename %></h1>
        
        <form action="/service-editor/<%= filename %>" method="POST">
            <textarea name="content" rows="20" class="w-full p-2 border rounded mb-4"><%= content %></textarea>
            <div class="flex justify-between">
                <a href="/service-editor" class="bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600">Cancel</a>
                <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Save Changes</button>
            </div>
        </form>
    </div>
    <script src="/js/navbar.js"></script>
</body>
</html>