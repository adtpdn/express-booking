<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
    <link href="/styles/style.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <%- include('partials/navbar') %>

    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold mb-6 text-red-600">Error</h1>
        <p class="mb-4"><%= message %></p>
        <% if (error) { %>
            <pre class="bg-gray-200 p-4 rounded"><%= error.stack %></pre>
        <% } %>
        <a href="/" class="mt-4 inline-block bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Go Back Home</a>
    </div>
    <script src="/js/navbar.js"></script>
</body>
</html>