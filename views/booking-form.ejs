<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Form</title>
    <link href="/styles/style.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold mb-6">Book <%= service.title %></h1>
        <form action="/submit-booking" method="POST" class="bg-white rounded-lg shadow-md p-6">
            <input type="hidden" name="service" value="<%= service.title %>">
            <div class="mb-4">
                <label for="date" class="block text-gray-700 font-bold mb-2">Date:</label>
                <input type="date" id="date" name="date" required class="w-full px-3 py-2 border rounded-lg">
            </div>
            <h3 class="text-lg font-semibold mb-2">Addons:</h3>
            <div class="space-y-2 mb-4">
                <% service.addons.forEach(addon => { %>
                    <label class="flex items-center">
                        <input type="checkbox" name="addons" value="<%= addon.name %>" class="mr-2">
                        <span><%= addon.name %> ($<%= addon.price %>)</span>
                    </label>
                <% }); %>
            </div>
            <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Book Now</button>
        </form>
    </div>
</body>
</html>