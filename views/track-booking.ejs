<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Track Booking</title>
    <link href="/styles/style.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold mb-6">Track Your Booking</h1>
        
        <% if (locals.error) { %>
            <p class="text-red-500 mb-4"><%= error %></p>
        <% } %>

        <form action="/track-booking" method="POST" class="bg-white rounded-lg shadow-md p-6">
            <div class="mb-4">
                <label for="bookingId" class="block text-gray-700 font-bold mb-2">Booking ID:</label>
                <input type="text" id="bookingId" name="bookingId" required class="w-full px-3 py-2 border rounded-lg">
            </div>
            <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Track Booking</button>
        </form>
    </div>
</body>
</html>